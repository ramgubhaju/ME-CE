# XOR Gate implementation using back propagation

## Configuration
The only configuration is the number of epoch, which is change in line `xor_obj.train(10000)`

## How to execute
```
python3 /path/to/your/xor_backprop.py
```